# In production the frontend is behind nginx, which proxies /api to the backend.
# Leave PUBLIC_API_URL empty so the WS client uses window.location.host (nginx).
# Set this to your backend URL only if you run the backend on a separate domain.
PUBLIC_APP_NAME=StarterKit
PUBLIC_API_URL=
